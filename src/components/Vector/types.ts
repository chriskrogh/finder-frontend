export type VectorIcon = {
  src: string;
  /** aspect ratio (width / height) */
  AR: number;
};

export type DesiredIconDimensions = {
  width?: number;
  height?: number;
  maxWidth?: number;
  maxHeight?: number;
};
